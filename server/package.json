{
  "name": "phantom-esports-payment-server",
  "version": "1.0.0",
  "description": "Minimal backend for creating Razorpay orders and verifying payment signatures for the PHANTOM ESPORTS registration site.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "razorpay": "^2.9.4",
    "resend": "^4.0.1"
  },
  "engines": {
    "node": ">=18"
  }
}
